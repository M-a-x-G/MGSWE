package NewLanguage.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class SpringPackaging_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<SpringPackaging> constants = ListSequence.fromList(SpringPackaging.getConstants()).iterator();
    while (constants.hasNext()) {
      SpringPackaging constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<SpringPackaging> constants = ListSequence.fromList(SpringPackaging.getConstants()).iterator();
    while (constants.hasNext()) {
      SpringPackaging constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    SpringPackaging constant = SpringPackaging.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
