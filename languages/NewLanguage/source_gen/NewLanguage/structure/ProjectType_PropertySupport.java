package NewLanguage.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class ProjectType_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<ProjectType> constants = ListSequence.fromList(ProjectType.getConstants()).iterator();
    while (constants.hasNext()) {
      ProjectType constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<ProjectType> constants = ListSequence.fromList(ProjectType.getConstants()).iterator();
    while (constants.hasNext()) {
      ProjectType constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    ProjectType constant = ProjectType.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
